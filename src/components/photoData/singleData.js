import React from 'react';


const Singledata = (props) => {
    console.log('single card', props)
    return(
        <>
        <p>{props.cardId}</p>

        </>
    )

}

export default Singledata;